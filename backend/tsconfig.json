{
  "compilerOptions": {
    "target": "ES6",                    // Целевая версия JavaScript
    "module": "ESNext",                 // Использование модулей ESNext
    "moduleResolution": "node",         // Разрешение модулей как в Node.js
    "esModuleInterop": true,            // Включить поддержку импорта CommonJS модулей
    "strict": true,                     // Включение строгого режима
    "skipLibCheck": true,               // Пропуск проверки типов в библиотеках
    "outDir": "./dist",                 // Папка для скомпилированных файлов
    "rootDir": "./",                    // Папка с исходными файлами (в вашем случае корень проекта)
    "baseUrl": ".",                     // Базовая директория для разрешения неявных модулей
    "paths": {                          // Псевдонимы путей для удобства
      "*": ["node_modules/*"]
    },
    "resolveJsonModule": true,          // Разрешение импорта JSON файлов
    "skipDefaultLibCheck": true,        // Пропуск проверки типов в библиотечных файлах
    "lib": ["esnext"],                  // Использование библиотеки для последней версии JavaScript
    "declaration": true,                // Генерация файлов .d.ts для типов
    "declarationMap": true              // Создание карт типов
  },
  "include": [
    "*.ts"                              // Включаем все .ts файлы в корне проекта
  ],
  "exclude": [
    "node_modules"                      // Исключаем папку node_modules
  ]
}
